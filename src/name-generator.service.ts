import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class NameGeneratorService {

  private names: string[] = ['Sara', 'Adara', 'Violeta'];

  constructor() { }

  generateRandomName(): string{
    const randomIndex = Math.floor(Math.)
  }
}
